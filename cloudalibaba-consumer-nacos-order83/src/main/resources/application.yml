server:
  port: 83

spring:
  application:
    name: nacos-order-consumer
  cloud:
    nacos:
      discovery:
        server-addr: localhost:8848


service-url:  #用来拼接服务获取地址
  nacos-user-service: http://nacos-payment-provider

#CAP原则又称CAP定理，指的是在一个分布式系统中，
#  Consistency（一致性）、
#  Availability（可用性）、
#  Partition tolerance（分区容错性），三者不可得兼